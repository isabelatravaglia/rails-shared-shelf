<div class="index-page">


  <%= form_tag books_path, method: :get do %>
    <%= text_field_tag :query,
    params[:query],
    class: "form-control",
    placeholder: "Find a book"
     %>
     <%= submit_tag "Search", class: "btn btn-dark rounded-pill" %>
  <% end %>

  <%= link_to "Add a book", new_book_path, class: 'btn btn-outline-secondary rounded-pill' %>

  <h2>Browse the Shelf</h2>


  <% if @results.empty? %>
    <%= "Sorry, this book doesn't exist in this plataform." %>
  <% else %>
    <% @results.each do |result|  %>
      <%= link_to book_path(result), method: :get, :action => 'go' do %>
        <div class="card-product">
          <img src= "<%= result.photo %>" />
          <div class="card-product-infos">
            <h2><%=result.name%>, <%=result.author%></h2>
            <p><%=result.description%></p>
            <p><%=result.language%></p>
            <p><%=result.publishing_year%></p>
          </div>
        </div>
        <% end %>
      <%end%>
    <% end %>

</div>



 <%#@books.each do |book|  %>

  <%#= link_to book_path(book), :action => 'go' do %>
    <!-- <div class="card-product"> -->
      <!-- <img src= "<%#= book.photo %>" /> -->
      <!-- <div class="card-product-infos"> -->
        <!-- <h2><%#=book.name%>, <%#=book.author%></h2> -->
         <!-- <p><%#=book.description%></p> -->
       <!-- </div>
     </div> -->
     <%# end %>
   <%#end%>
